<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.product.margin</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="sale.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//setting[@name='request_vendor_setting_container']" position="after">
                <block title="Margin" name="product_sales_margin_min_max">
                    <setting id="sale_discount_limit"
                            documentation="https://www.odoo.com/documentation/17.0"
                            title="User can limit the discount amount in the sale order line."
                            invisible="not group_discount_per_so_line"
                            help="User can limit the discount amount">
                        <field name="is_discount_limit" />
                        <div class="content-group"
                            invisible="not is_discount_limit">
                            <div class="mt16">
                                <label for="discount_limit" />
                                <field name="discount_limit" />
                            </div>
                        </div>
                    </setting>
                    <setting
                        title="This adds a dropshipping route to apply on products in order to request your vendors to deliver to your customers. A product to dropship will generate a purchase request for quotation once the sales order confirmed. This is a on-demand flow. The requested delivery address will be the customer delivery address and not your warehouse."
                        help="Request your vendors to deliver to your customers"
                        documentation="/applications/inventory_and_mrp/inventory/management/delivery/dropshipping.html">
                        <field name="module_stock_dropshipping" />
                    </setting>
                </block>
            </xpath>
        </field>
    </record>
</odoo>